class Solution:
    def containsDuplicate(self, nums: List[int]) -> bool:
        # a=len(nums)
        # for i in range(a):
        #     for j in range(i+1,a):
        #         if nums[i]== nums[j]:
        #             return True
        # return False
        
        for i in range(len(nums)):
            n=nums[i]
            if n in nums[i+1:]:
                return True
        return False
